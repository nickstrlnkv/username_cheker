# Telegram Username Monitor Bot

Бот для непрерывного мониторинга освобождения Telegram username с мгновенными уведомлениями.

## Возможности

- ✅ Непрерывный мониторинг 24/7
- ✅ Проверка до 1000 username за цикл
- ✅ Асинхронная обработка через Telethon
- ✅ Загрузка/выгрузка базы username
- ✅ Управление через удобный интерфейс
- ✅ Статистика и настройки
- ✅ Мгновенные уведомления при освобождении
- ✅ Спам уведомлений в чат при освобождении username

## Установка

### 1. Клонирование репозитория

```bash
git clone <repo_url>
cd username_cheker
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Получение API ключей

#### Telegram Bot Token
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

#### Telegram API ID и API Hash
1. Перейдите на https://my.telegram.org
2. Войдите с вашим номером телефона
3. Перейдите в "API development tools"
4. Создайте новое приложение
5. Скопируйте `api_id` и `api_hash`

#### Admin ID
1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение
3. Скопируйте ваш `Id`

### 4. Настройка конфигурации

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните `.env` вашими данными:

```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
ADMIN_ID=123456789
```

**Примечание:** Настройки спама (задержка, количество сообщений, режим) настраиваются через интерфейс бота в разделе "⚙ Настройки" → "💬 Настройки спама". Бот автоматически будет спамить в тот чат, где администратор запустил мониторинг.

## Запуск

### Windows

```bash
# Активация venv
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

# Запуск
python bot.py
```

Или используйте `start.bat` для быстрого запуска.

### Linux

```bash
# Установка и настройка
chmod +x setup.sh
./setup.sh

# Запуск
chmod +x start.sh
./start.sh
```

**Подробная инструкция по развертыванию на Linux сервере: [DEPLOY.md](DEPLOY.md)**

### Авторизация Telethon

При первом запуске бот автоматически запросит авторизацию **через Telegram**:

1. Бот отправит сообщение: "📱 Авторизация Telethon"
2. Отправьте ваш номер телефона (например: `+79991234567`)
3. Бот попросит код - отправьте код из Telegram
4. Если включена 2FA - отправьте пароль (сообщение автоматически удалится)

**Важно:** Все действия происходят в чате с ботом, терминал не используется!

## Использование

### Команды бота

- `/start` - Главное меню

### Интерфейс

**Управление мониторингом:**
- ▶ **Старт** - Запустить мониторинг
- ⏹ **Стоп** - Остановить мониторинг

**Работа с базой:**
- 📥 **Загрузить базу** - Загрузить username из .txt/.csv файла
- 📤 **Выгрузить базу** - Скачать текущую базу
- ➕ **Добавить username** - Добавить username вручную
- ➖ **Удалить username** - Удалить username из базы
- 🗑 **Очистить базу** - Удалить все username

**Информация:**
- 📊 **Статистика** - Показать статистику проверок
- ⚙ **Настройки** - Изменить параметры мониторинга
  - ⏱ Интервал проверки - задержка между батчами
  - 📦 Размер батча - количество username в одном батче
  - 💬 Настройки спама - настройка уведомлений при освобождении username
    - ⏱ Задержка между сообщениями
    - 🔢 Количество сообщений (для режима "Указать количество")
    - 🔄 Режим спама: "Указать количество" или "До занятия username"

### Формат файлов для загрузки

**Простой список (.txt):**
```
@username1
username2
@username3
```

**CSV формат (.csv):**
```
@username1
username2
username3
```

Поддерживается:
- С символом `@` или без
- Комментарии начинающиеся с `#`
- Пустые строки игнорируются

## Архитектура

```
├── bot.py              # Главный файл запуска
├── config.py           # Конфигурация
├── database.py         # Работа с SQLite
├── checker.py          # Логика проверки через Telethon
├── handlers.py         # Обработчики команд бота
├── keyboards.py        # Клавиатуры интерфейса
├── utils.py            # Вспомогательные функции
└── requirements.txt    # Зависимости
```

## Технические детали

### Производительность

- **Асинхронная обработка**: до 20-50 username параллельно
- **Батчинг**: обработка по 50 username за раз
- **Целевая скорость**: 1000 username за 5-10 секунд
- **Rate limiting**: автоматическая обработка FloodWait

### База данных

SQLite с тремя таблицами:
- `usernames` - список отслеживаемых username
- `settings` - настройки мониторинга
- `logs` - история изменений статусов

### Логирование

Логи сохраняются в папку `logs/`:
- Формат: `bot_YYYYMMDD.log`
- Уровень: INFO и выше
- Ротация: новый файл каждый день

## Безопасность

⚠️ **Важно:**
- Не публикуйте файл `.env`
- Не делитесь токенами и API ключами
- Файл `.session` содержит авторизацию - храните в безопасности
- Добавьте `.env`, `*.session`, `*.db` в `.gitignore`

## Устранение неполадок

### Ошибка "BOT_TOKEN not found"
Убедитесь, что создан файл `.env` с правильными данными.

### Ошибка "FloodWait"
Telegram временно ограничил запросы. Бот автоматически подождет и продолжит работу.

### Бот не отвечает
Проверьте:
1. Правильность BOT_TOKEN
2. Интернет соединение
3. Логи в папке `logs/`

### Telethon не авторизуется
1. Удалите файл `checker_session.session`
2. Перезапустите бота
3. Пройдите авторизацию заново

## Лицензия

MIT License

## Поддержка

При возникновении проблем создайте Issue в репозитории.
